# =============================================================================
# UNIFIED PRIVACY PATCHES
# =============================================================================
#
# Comprehensive privacy hardening for codex-rs across all supported versions.
# Each patch has an optional `version` field for version-specific constraints.
# Patches without a version field apply to all versions.
#
# Supported version ranges:
# - 0.99.0-alpha.14 through 0.104.x
# - 0.105.0-alpha.13 through 0.107.x
# - 0.108.0-alpha.1 and later
#
# Categories:
# 1. Statsig telemetry (disable metrics export)
# 2. Turn metadata (disable git/sandbox headers)
# 3. Analytics (disable skill/app tracking)
# 4. Realtime conversation (remove config overrides, debug logging)
# 5. Web search (default disabled)
# 6. Sandbox metrics (remove tags)
# 7. Git tracking (redact origin URL)
# 8. OAuth (enforce loopback)
# 9. External logos (remove query)
# 10. Config hardening (force non-login shell, etc.)
#
# =============================================================================

[meta]
name = "privacy"
description = "Comprehensive privacy hardening for codex-rs"
workspace_relative = true
# No version_range - file applies to all, individual patches have constraints

# =============================================================================
# 1. STATSIG TELEMETRY - Disable metrics export to external service
# =============================================================================

[[patches]]
id = "privacy-disable-statsig-resolver"
file = "otel/src/config.rs"

[patches.query]
type = "ast-grep"
pattern = '''
pub(crate) fn resolve_exporter(exporter: &OtelExporter) -> OtelExporter {
    $$$
}
'''

[patches.operation]
type = "replace"
text = '''
pub(crate) fn resolve_exporter(exporter: &OtelExporter) -> OtelExporter {
    match exporter {
        OtelExporter::Statsig => {
            // PRIVACY PATCH: Always return None to disable Statsig telemetry.
            OtelExporter::None
        }
        _ => exporter.clone(),
    }
}
'''

[[patches]]
id = "privacy-remove-statsig-endpoint"
file = "otel/src/config.rs"

[patches.query]
type = "ast-grep"
pattern = '''
pub(crate) const STATSIG_OTLP_HTTP_ENDPOINT: $TYPE = $VALUE;
'''

[patches.operation]
type = "delete"
insert_comment = "// PRIVACY PATCH: Statsig endpoint removed"

[[patches]]
id = "privacy-remove-statsig-header"
file = "otel/src/config.rs"

[patches.query]
type = "ast-grep"
pattern = '''
pub(crate) const STATSIG_API_KEY_HEADER: $TYPE = $VALUE;
'''

[patches.operation]
type = "delete"
insert_comment = "// PRIVACY PATCH: Statsig API key header removed"

[[patches]]
id = "privacy-remove-statsig-api-key"
file = "otel/src/config.rs"

[patches.query]
type = "ast-grep"
pattern = '''
pub(crate) const STATSIG_API_KEY: $TYPE = $VALUE;
'''

[patches.operation]
type = "delete"
insert_comment = "// PRIVACY PATCH: Statsig API key removed"

[[patches]]
id = "privacy-default-metrics-none-types"
file = "core/src/config/types.rs"

[patches.query]
type = "ast-grep"
pattern = '''
impl Default for OtelConfig {
    fn default() -> Self {
        $$$
    }
}
'''

[patches.operation]
type = "replace"
text = '''
impl Default for OtelConfig {
    fn default() -> Self {
        OtelConfig {
            log_user_prompt: false,
            environment: DEFAULT_OTEL_ENVIRONMENT.to_owned(),
            exporter: OtelExporterKind::None,
            trace_exporter: OtelExporterKind::None,
            metrics_exporter: OtelExporterKind::None, // PRIVACY PATCH: Changed from Statsig
        }
    }
}
'''

[[patches]]
id = "privacy-config-metrics-exporter-default-none"
file = "core/src/config/mod.rs"

[patches.query]
type = "text"
search = '''
                let metrics_exporter = t.metrics_exporter.unwrap_or(OtelExporterKind::Statsig);
'''

[patches.operation]
type = "replace"
text = '''
                let metrics_exporter = t.metrics_exporter.unwrap_or(OtelExporterKind::None); // PRIVACY PATCH
'''

# =============================================================================
# 2. TURN METADATA - Disable git/sandbox headers
# =============================================================================

[[patches]]
id = "privacy-build-turn-metadata-header-noop"
version = "<0.108.0-alpha.1"
file = "core/src/turn_metadata.rs"

[patches.query]
type = "ast-grep"
pattern = '''
pub fn build_turn_metadata_header($$$PARAMS) -> Option<String> {
    $$$BODY
}
'''

[patches.operation]
type = "replace"
text = '''
pub fn build_turn_metadata_header() -> Option<String> {
    // PRIVACY PATCH: Disable per-turn metadata headers.
    None
}
'''

[[patches]]
id = "privacy-turn-metadata-state-current-header-always-none"
file = "core/src/turn_metadata.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
    pub fn current_header(&self) -> Option<String> {
        self.enriched_header.clone()
    }
'''

[patches.operation]
type = "replace"
text = '''
    pub fn current_header(&self) -> Option<String> {
        // PRIVACY PATCH: Always return None to disable turn metadata headers.
        let _ = &self.enriched_header; // Suppress unused field warning
        None
    }
'''

[[patches]]
id = "privacy-turn-metadata-state-disable-enrichment"
file = "core/src/turn_metadata.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
    pub fn set_enriched_header(&mut self, header: Option<String>) {
        self.enriched_header = header;
    }
'''

[patches.operation]
type = "replace"
text = '''
    pub fn set_enriched_header(&mut self, header: Option<String>) {
        // PRIVACY PATCH: Discard enriched header to prevent metadata emission.
        let _ = header;
        self.enriched_header = None;
    }
'''

[[patches]]
id = "privacy-remove-turn-metadata-header-constant"
file = "core/src/client.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
pub const TURN_METADATA_HEADER: &str = "x-codex-turn-metadata";
'''

[patches.operation]
type = "replace"
text = '''
// PRIVACY PATCH: Turn metadata header constant removed.
'''

[[patches]]
id = "privacy-disable-http-turn-metadata-header-insertion"
file = "core/src/client.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
        if let Some(metadata) = &self.turn_metadata {
            request = request.header(TURN_METADATA_HEADER, metadata);
        }
'''

[patches.operation]
type = "replace"
text = '''
        if let Some(metadata) = &self.turn_metadata {
            // PRIVACY PATCH: Do not send turn metadata header.
            let _ = metadata;
        }
'''

# =============================================================================
# 3. ANALYTICS - Disable skill/app tracking
# =============================================================================

[[patches]]
id = "privacy-analytics-track-skill-noop"
file = "core/src/analytics_client.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
pub(crate) fn track_skill_invocations(
    queue: &AnalyticsEventsQueue,
    config: Arc<Config>,
    tracking: TrackEventsContext,
    invocations: Vec<SkillInvocation>,
) {
    queue.maybe_enqueue(TrackEventsJob::SkillInvocations(TrackSkillInvocationsJob {
        config,
        tracking,
        invocations,
    }));
}
'''

[patches.operation]
type = "replace"
text = '''
pub(crate) fn track_skill_invocations(
    queue: &AnalyticsEventsQueue,
    config: Arc<Config>,
    tracking: TrackEventsContext,
    invocations: Vec<SkillInvocation>,
) {
    // PRIVACY PATCH: Disable skill invocation analytics.
    let _ = (queue, config, tracking, invocations);
}
'''

[[patches]]
id = "privacy-analytics-track-app-mentioned-noop"
file = "core/src/analytics_client.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
pub(crate) fn track_app_mentioned(
    queue: &AnalyticsEventsQueue,
    config: Arc<Config>,
    tracking: TrackEventsContext,
    mentions: Vec<AppInvocation>,
) {
    queue.maybe_enqueue(TrackEventsJob::AppMentioned(TrackAppMentionedJob {
        config,
        tracking,
        mentions,
    }));
}
'''

[patches.operation]
type = "replace"
text = '''
pub(crate) fn track_app_mentioned(
    queue: &AnalyticsEventsQueue,
    config: Arc<Config>,
    tracking: TrackEventsContext,
    mentions: Vec<AppInvocation>,
) {
    // PRIVACY PATCH: Disable app mentioned analytics.
    let _ = (queue, config, tracking, mentions);
}
'''

[[patches]]
id = "privacy-analytics-track-app-used-noop"
file = "core/src/analytics_client.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
pub(crate) fn track_app_used(
    queue: &AnalyticsEventsQueue,
    config: Arc<Config>,
    tracking: TrackEventsContext,
    app: AppInvocation,
) {
    queue.maybe_enqueue(TrackEventsJob::AppUsed(TrackAppUsedJob {
        config,
        tracking,
        app,
    }));
}
'''

[patches.operation]
type = "replace"
text = '''
pub(crate) fn track_app_used(
    queue: &AnalyticsEventsQueue,
    config: Arc<Config>,
    tracking: TrackEventsContext,
    app: AppInvocation,
) {
    // PRIVACY PATCH: Disable app used analytics.
    let _ = (queue, config, tracking, app);
}
'''

# =============================================================================
# 4. REALTIME CONVERSATION - Remove config overrides and debug logging
# =============================================================================

# 0.105-0.107: Different auth signature
[[patches]]
id = "privacy-realtime-remove-overrides-v105"
file = "core/src/realtime_conversation.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
    let config = sess.get_config().await;
    if let Some(realtime_ws_base_url) = &config.experimental_realtime_ws_base_url {
        api_provider.base_url = realtime_ws_base_url.clone();
    }
    let prompt = config
        .experimental_realtime_ws_backend_prompt
        .clone()
        .unwrap_or(params.prompt);
'''

[patches.operation]
type = "replace"
text = '''
    let _config = sess.get_config().await;
    // PRIVACY PATCH: Removed experimental realtime config overrides.
    let prompt = params.prompt;
'''

# 0.108+: New auth signature and experimental_realtime_ws_model field
[[patches]]
id = "privacy-realtime-remove-overrides-v108"
file = "core/src/realtime_conversation.rs"
version = ">=0.108.0-alpha.1"

[patches.query]
type = "text"
fuzzy_expansion = 15
search = '''
    let config = sess.get_config().await;
    if let Some(realtime_ws_base_url) = &config.experimental_realtime_ws_base_url {
        api_provider.base_url = realtime_ws_base_url.clone();
    }
    let prompt = config
        .experimental_realtime_ws_backend_prompt
        .clone()
        .unwrap_or(params.prompt);
    let prompt =
        match build_realtime_startup_context(sess.as_ref(), REALTIME_STARTUP_CONTEXT_TOKEN_BUDGET)
            .await
        {
            Some(context) => format!("{prompt}\n\n{context}"),
            None => prompt,
        };
    let model = config.experimental_realtime_ws_model.clone();
'''

[patches.operation]
type = "replace"
text = '''
    let config = sess.get_config().await;
    // PRIVACY PATCH: Removed experimental realtime config overrides.
    let prompt = params.prompt;
    let prompt =
        match build_realtime_startup_context(sess.as_ref(), REALTIME_STARTUP_CONTEXT_TOKEN_BUDGET)
            .await
        {
            Some(context) => format!("{prompt}\n\n{context}"),
            None => prompt,
        };
    let model = config.experimental_realtime_ws_model.clone();
'''

[[patches]]
id = "privacy-realtime-remove-text-output-debug-log"
file = "core/src/realtime_conversation.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
                debug!(text = %text, "[realtime-text] realtime conversation text output");
'''

[patches.operation]
type = "replace"
text = '''
                // PRIVACY PATCH: Removed realtime text-output debug logging.
'''

[[patches]]
id = "privacy-realtime-remove-text-input-debug-log"
file = "core/src/realtime_conversation.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
    debug!(text = %params.text, "[realtime-text] appending realtime conversation text input");
'''

[patches.operation]
type = "replace"
text = '''
    // PRIVACY PATCH: Removed realtime text-input debug logging.
'''

# =============================================================================
# 5. WEB SEARCH - Default disabled
# =============================================================================

[[patches]]
id = "privacy-web-search-default-disabled"
file = "core/src/config/mod.rs"
version = ">=0.99.0-alpha.14, <0.105.0-alpha.13"

[patches.query]
type = "text"
search = '''
        let web_search_mode = resolve_web_search_mode(&cfg, &config_profile, &features)
            .unwrap_or(WebSearchMode::Cached);
'''

[patches.operation]
type = "replace"
text = '''
        let web_search_mode = resolve_web_search_mode(&cfg, &config_profile, &features)
            .unwrap_or(WebSearchMode::Disabled); // PRIVACY PATCH: Default to Disabled
'''

[[patches]]
id = "privacy-web-search-no-sandbox-upgrade"
file = "core/src/config/mod.rs"
version = ">=0.99.0-alpha.14, <0.105.0-alpha.13"

[patches.query]
type = "ast-grep"
pattern = '''
pub(crate) fn resolve_web_search_mode_for_turn($$$PARAMS) -> WebSearchMode {
    $$$BODY
}
'''

[patches.operation]
type = "replace"
text = '''
pub(crate) fn resolve_web_search_mode_for_turn(
    web_search_mode: &Constrained<WebSearchMode>,
    _sandbox_policy: &SandboxPolicy,
) -> WebSearchMode {
    // PRIVACY PATCH: Do not upgrade to Live based on sandbox policy.
    let preferred = web_search_mode.value();
    if web_search_mode.can_set(&preferred).is_ok() {
        return preferred;
    }
    WebSearchMode::Disabled
}
'''

# =============================================================================
# 6. SANDBOX METRICS - Remove tags from tool telemetry
# =============================================================================

[[patches]]
id = "privacy-drop-tool-sandbox-metric-tags"
file = "core/src/tools/registry.rs"
version = ">=0.99.0-alpha.14, <0.105.0-alpha.13"

[patches.query]
type = "ast-grep"
pattern = '''
let metric_tags = [
    (
        "sandbox",
        sandbox_tag($$$SANDBOX_ARGS),
    ),
    (
        "sandbox_policy",
        sandbox_policy_tag($$$POLICY_ARGS),
    ),
];
'''

[patches.operation]
type = "replace"
text = '''
        let _ = (sandbox_tag, sandbox_policy_tag); // PRIVACY PATCH: Keep symbols referenced
        let metric_tags: [(&str, &str); 0] = []; // PRIVACY PATCH: Drop sandbox tags
'''

# =============================================================================
# 7. GIT TRACKING - Redact origin URL
# =============================================================================

[[patches]]
id = "privacy-redact-git-origin-url-state"
file = "state/src/extract.rs"
version = ">=0.99.0-alpha.14, <0.105.0-alpha.13"

[patches.query]
type = "text"
search = '''
    if let Some(git) = meta_line.git.as_ref() {
        metadata.git_sha = git.commit_hash.clone();
        metadata.git_branch = git.branch.clone();
        metadata.git_origin_url = git.repository_url.clone();
    }
'''

[patches.operation]
type = "replace"
text = '''
    if let Some(git) = meta_line.git.as_ref() {
        metadata.git_sha = git.commit_hash.clone();
        metadata.git_branch = git.branch.clone();
        // PRIVACY PATCH: Redact git origin URL
        metadata.git_origin_url = None;
    }
'''

[[patches]]
id = "privacy-redact-git-origin-url-rollout"
file = "core/src/rollout/metadata.rs"
version = ">=0.99.0-alpha.14, <0.105.0-alpha.13"

[patches.query]
type = "text"
search = '''
    if let Some(git) = session_meta.git.as_ref() {
        builder.git_sha = git.commit_hash.clone();
        builder.git_branch = git.branch.clone();
        builder.git_origin_url = git.repository_url.clone();
    }
'''

[patches.operation]
type = "replace"
text = '''
    if let Some(git) = session_meta.git.as_ref() {
        builder.git_sha = git.commit_hash.clone();
        builder.git_branch = git.branch.clone();
        // PRIVACY PATCH: Redact git origin URL
        builder.git_origin_url = None;
    }
'''

# =============================================================================
# 8. OAUTH - Enforce loopback-only callbacks
# =============================================================================

[[patches]]
id = "privacy-oauth-enforce-loopback-callback-url-host"
file = "rmcp-client/src/perform_oauth_login.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
    let callback_url = callback_url.as_str();
'''

[patches.operation]
type = "replace"
text = '''
    // PRIVACY PATCH: Enforce loopback-only callback URL host.
    fn is_loopback_host(url: &str) -> bool {
        url::Url::parse(url)
            .map(|u| {
                u.host_str()
                    .map(|h| h == "127.0.0.1" || h == "localhost" || h == "::1")
                    .unwrap_or(false)
            })
            .unwrap_or(false)
    }
    if !is_loopback_host(callback_url.as_str()) {
        return Err(anyhow::anyhow!("PRIVACY: OAuth callback URL must use loopback host"));
    }
    let callback_url = callback_url.as_str();
'''

# =============================================================================
# 9. EXTERNAL LOGOS - Remove query from connectors
# =============================================================================

[[patches]]
id = "privacy-connectors-remove-external-logos-query"
file = "chatgpt/src/connectors.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
    let url = format!(
        "{}/codex/connectors/all?include_icons=true&external_logos=true",
        base_url.trim_end_matches('/')
    );
'''

[patches.operation]
type = "replace"
text = '''
    // PRIVACY PATCH: Removed external_logos query parameter.
    let url = format!(
        "{}/codex/connectors/all?include_icons=true",
        base_url.trim_end_matches('/')
    );
'''

[[patches]]
id = "privacy-connectors-remove-workspace-external-logos-query"
file = "chatgpt/src/connectors.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
    let url = format!(
        "{}/codex/connectors/workspace?workspace_id={}&include_icons=true&external_logos=true",
        base_url.trim_end_matches('/'),
        workspace_id
    );
'''

[patches.operation]
type = "replace"
text = '''
    // PRIVACY PATCH: Removed external_logos query parameter.
    let url = format!(
        "{}/codex/connectors/workspace?workspace_id={}&include_icons=true",
        base_url.trim_end_matches('/'),
        workspace_id
    );
'''

# =============================================================================
# 10. CONFIG HARDENING - Force non-login shell, etc.
# =============================================================================

[[patches]]
id = "privacy-config-force-login-shell-disabled"
file = "core/src/config/mod.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
            allow_login_shell: cfg.allow_login_shell,
'''

[patches.operation]
type = "replace"
text = '''
            allow_login_shell: false, // PRIVACY PATCH: Force non-login shell
'''

[[patches]]
id = "privacy-app-server-log-format-env-ignored"
file = "app-server/src/lib.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
    let log_format = std::env::var("LOG_FORMAT")
        .ok()
        .and_then(|s| s.parse::<LogFormat>().ok())
        .unwrap_or(LogFormat::Json);
'''

[patches.operation]
type = "replace"
text = '''
    // PRIVACY PATCH: Ignore LOG_FORMAT env override, always use Json.
    let log_format = LogFormat::Json;
'''

# =============================================================================
# 11. NETWORK PROXY - Disable audit events
# =============================================================================

[[patches]]
id = "privacy-network-proxy-disable-policy-audit-events"
file = "network-proxy/src/network_policy.rs"
version = ">=0.105.0-alpha.13, <0.108.0-alpha.1"

[patches.query]
type = "text"
search = '''
    fn emit_policy_decision_event(&self, _policy_event: PolicyDecision) {
        // TODO: Implement policy audit event emission
    }
'''

[patches.operation]
type = "replace"
text = '''
    fn emit_policy_decision_event(&self, _policy_event: PolicyDecision) {
        // PRIVACY PATCH: Policy audit events disabled.
    }
'''

# =============================================================================
# V0.108+ PRIVACY PATCHES
# =============================================================================
#
# Patches below this line are for codex-rs v0.108.0-alpha.1 and later.
# They use updated patterns matching the v0.108 codebase structure.
# =============================================================================

# =============================================================================
# 12. TURN METADATA v0.108 - Disable all turn metadata header generation
# =============================================================================

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-turn-metadata-header-noop"
file = "core/src/turn_metadata.rs"

[patches.query]
type = "ast-grep"
pattern = '''
pub async fn build_turn_metadata_header($$$PARAMS) -> Option<String> {
    $$$BODY
}
'''

[patches.operation]
type = "replace"
text = '''
pub async fn build_turn_metadata_header(_cwd: &Path, _sandbox: Option<&str>) -> Option<String> {
    // PRIVACY PATCH: Disable turn metadata header generation.
    None
}
'''

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-turn-metadata-state-always-none"
file = "core/src/turn_metadata.rs"

[patches.query]
type = "ast-grep"
pattern = '''
pub(crate) fn current_header_value(&self) -> Option<String> {
    $$$BODY
}
'''

[patches.operation]
type = "replace"
text = '''
pub(crate) fn current_header_value(&self) -> Option<String> {
    // PRIVACY PATCH: Always return None to disable turn metadata headers.
    let _ = &self.enriched_header;
    let _ = &self.base_header;
    None
}
'''

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-turn-metadata-disable-enrichment"
file = "core/src/turn_metadata.rs"

[patches.query]
type = "ast-grep"
pattern = '''
pub(crate) fn spawn_git_enrichment_task(&self) {
    $$$BODY
}
'''

[patches.operation]
type = "replace"
text = '''
pub(crate) fn spawn_git_enrichment_task(&self) {
    // PRIVACY PATCH: Disable git metadata enrichment.
    let _ = &self.repo_root;
}
'''

# =============================================================================
# 1b. TURN METADATA - Remove header insertion in client.rs
# =============================================================================

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-client-ws-metadata-noop"
file = "core/src/client.rs"

[patches.query]
type = "ast-grep"
pattern = '''
fn build_ws_client_metadata(turn_metadata_header: Option<&str>) -> Option<HashMap<String, String>> {
    $$$BODY
}
'''

[patches.operation]
type = "replace"
text = '''
fn build_ws_client_metadata(_turn_metadata_header: Option<&str>) -> Option<HashMap<String, String>> {
    // PRIVACY PATCH: Don't send turn metadata in websocket client metadata.
    None
}
'''

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-client-responses-headers-no-metadata"
file = "core/src/client.rs"

[patches.query]
type = "ast-grep"
pattern = '''
fn build_responses_headers(
    beta_features_header: Option<&str>,
    turn_state: Option<&Arc<OnceLock<String>>>,
    turn_metadata_header: Option<&HeaderValue>,
) -> ApiHeaderMap {
    $$$BODY
}
'''

[patches.operation]
type = "replace"
text = '''
fn build_responses_headers(
    beta_features_header: Option<&str>,
    turn_state: Option<&Arc<OnceLock<String>>>,
    _turn_metadata_header: Option<&HeaderValue>,
) -> ApiHeaderMap {
    // PRIVACY PATCH: Removed turn metadata header insertion.
    let mut headers = ApiHeaderMap::new();
    if let Some(value) = beta_features_header
        && !value.is_empty()
        && let Ok(header_value) = HeaderValue::from_str(value)
    {
        headers.insert("x-codex-beta-features", header_value);
    }
    if let Some(turn_state) = turn_state
        && let Some(state) = turn_state.get()
        && let Ok(header_value) = HeaderValue::from_str(state)
    {
        headers.insert(X_CODEX_TURN_STATE_HEADER, header_value);
    }
    headers
}
'''

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-lib-remove-turn-metadata-export"
file = "core/src/lib.rs"

[patches.query]
type = "text"
search = 'pub use client::X_CODEX_TURN_METADATA_HEADER;'

[patches.operation]
type = "replace"
text = '// PRIVACY PATCH: X_CODEX_TURN_METADATA_HEADER export removed.'

# =============================================================================
# 2. ANALYTICS - Disable skill and app tracking (free functions only)
# =============================================================================

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-analytics-track-skill-noop"
file = "core/src/analytics_client.rs"

[patches.query]
type = "ast-grep"
pattern = '''
pub(crate) fn track_skill_invocations(
    queue: &AnalyticsEventsQueue,
    config: Arc<Config>,
    tracking: Option<TrackEventsContext>,
    invocations: Vec<SkillInvocation>,
) {
    $$$BODY
}
'''

[patches.operation]
type = "replace"
text = '''
pub(crate) fn track_skill_invocations(
    _queue: &AnalyticsEventsQueue,
    _config: Arc<Config>,
    _tracking: Option<TrackEventsContext>,
    _invocations: Vec<SkillInvocation>,
) {
    // PRIVACY PATCH: Disable skill invocation tracking.
}
'''

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-analytics-track-app-mentioned-noop"
file = "core/src/analytics_client.rs"

[patches.query]
type = "ast-grep"
pattern = '''
pub(crate) fn track_app_mentioned(
    queue: &AnalyticsEventsQueue,
    config: Arc<Config>,
    tracking: Option<TrackEventsContext>,
    mentions: Vec<AppInvocation>,
) {
    $$$BODY
}
'''

[patches.operation]
type = "replace"
text = '''
pub(crate) fn track_app_mentioned(
    _queue: &AnalyticsEventsQueue,
    _config: Arc<Config>,
    _tracking: Option<TrackEventsContext>,
    _mentions: Vec<AppInvocation>,
) {
    // PRIVACY PATCH: Disable app mentioned tracking.
}
'''

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-analytics-track-app-used-noop"
file = "core/src/analytics_client.rs"

[patches.query]
type = "ast-grep"
pattern = '''
pub(crate) fn track_app_used(
    queue: &AnalyticsEventsQueue,
    config: Arc<Config>,
    tracking: Option<TrackEventsContext>,
    app: AppInvocation,
) {
    $$$BODY
}
'''

[patches.operation]
type = "replace"
text = '''
pub(crate) fn track_app_used(
    _queue: &AnalyticsEventsQueue,
    _config: Arc<Config>,
    _tracking: Option<TrackEventsContext>,
    _app: AppInvocation,
) {
    // PRIVACY PATCH: Disable app used tracking.
}
'''

# =============================================================================
# 3. CONNECTORS - Remove external logo queries
# =============================================================================

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-connectors-remove-external-logos-directory"
file = "chatgpt/src/connectors.rs"

[patches.query]
type = "text"
search = 'None => "/connectors/directory/list?tier=categorized&external_logos=true".to_string(),'

[patches.operation]
type = "replace"
text = 'None => "/connectors/directory/list?tier=categorized".to_string(), // PRIVACY: no external_logos'

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-connectors-remove-external-logos-workspace"
file = "chatgpt/src/connectors.rs"

[patches.query]
type = "text"
search = '"/connectors/directory/list_workspace?external_logos=true".to_string(),'

[patches.operation]
type = "replace"
text = '"/connectors/directory/list_workspace".to_string(), // PRIVACY: no external_logos'

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-connectors-remove-external-logos-paged"
file = "chatgpt/src/connectors.rs"

[patches.query]
type = "text"
search = '"/connectors/directory/list?tier=categorized&token={encoded_token}&external_logos=true"'

[patches.operation]
type = "replace"
text = '"/connectors/directory/list?tier=categorized&token={encoded_token}" // PRIVACY: no external_logos'

# =============================================================================
# 4. NETWORK PROXY - Disable policy audit events
# =============================================================================

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-network-proxy-disable-audit-events"
file = "network-proxy/src/network_policy.rs"

[patches.query]
type = "ast-grep"
pattern = '''
fn emit_policy_audit_event(state: &NetworkProxyState, args: PolicyAuditEventArgs<'_>) {
    $$$BODY
}
'''

[patches.operation]
type = "replace"
text = '''
fn emit_policy_audit_event(_state: &NetworkProxyState, _args: PolicyAuditEventArgs<'_>) {
    // PRIVACY PATCH: Policy audit events disabled.
}
'''

# =============================================================================
# 5. APP SERVER - Ignore log format environment variable
# =============================================================================

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-app-server-log-format-env-ignored"
file = "app-server/src/lib.rs"

[patches.query]
type = "ast-grep"
pattern = '''
fn log_format_from_env() -> LogFormat {
    $$$BODY
}
'''

[patches.operation]
type = "replace"
text = '''
fn log_format_from_env() -> LogFormat {
    // PRIVACY PATCH: Ignore LOG_FORMAT env var, always use default.
    LogFormat::Default
}
'''

# =============================================================================
# 6. CONFIG - Disable login shell by default
# =============================================================================

[[patches]]
version = ">=0.108.0-alpha.1"
id = "privacy-v108-config-disable-login-shell"
file = "core/src/config/mod.rs"

[patches.query]
type = "text"
search = 'let allow_login_shell = cfg.allow_login_shell.unwrap_or(true);'

[patches.operation]
type = "replace"
text = 'let allow_login_shell = cfg.allow_login_shell.unwrap_or(false); // PRIVACY: default off'
